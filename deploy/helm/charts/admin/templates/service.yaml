apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin.fullname" . }}
  labels:
    app: {{ include "admin.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "admin.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
