apiVersion: v1
kind: Service
metadata:
  name: {{ include "logistics.fullname" . }}
  labels:
    app: {{ include "logistics.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "logistics.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 4400
      protocol: TCP
      name: http
