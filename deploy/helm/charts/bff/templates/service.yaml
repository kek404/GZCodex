apiVersion: v1
kind: Service
metadata:
  name: {{ include "bff.fullname" . }}
  labels:
    app: {{ include "bff.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "bff.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 4000
      protocol: TCP
      name: http
